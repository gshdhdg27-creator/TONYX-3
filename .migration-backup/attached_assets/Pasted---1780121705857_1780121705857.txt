Привет! Отличная работа, вкладка админки появилась и скрыта от обычных пользователей, тут всё супер. Но нужно убрать лишнее и реализовать крупный блок новых фич (Рефералка, Инвестиции, перенос рекламы).

Выполни следующие задачи:

1. Убрать защиту в админке:
   - Внутри панели администратора сейчас стоит заглушка "Введите ваш ID". Она работает некорректно и ничего не открывает. УБЕРИ эту форму ввода полностью. Раз пользователь уже зашел во вкладку (а туда пускает только ID 7257793582 или isAdmin), значит он проверен. Админка должна открываться СРАЗУ.

2. Настройка команды /start в Боте:
   - Убедись, что в коде телеграм-бота (папка api-server, библиотека telegraf) команда `/start` работает корректно, отправляет приветственное сообщение и правильно обрабатывает реферальные хвосты типа `/start ref_123456`.

3. Реферальная система (10% от дохода):
   - Реализуй реферальную привязку при регистрации через `/start`.
   - Напиши логику: когда пользователь выполняет задание с просмотром рекламы (в разделе задач) и получает награду, 10% от этой суммы (в "шпротах") должны автоматически начисляться на баланс пользователя, который его пригласил.

4. Перенос рекламы с Главного экрана:
   - Полностью удали блоки рекламы с Главного экрана (Home/Main screen).
   - Перенеси всю рекламу в раздел Заданий (Tasks tab), чтобы пользователи смотрели её там ради заработка.

5. Раздел "Инвестиции" на Главном экране (Капание каждую миллисекунду):
   - На главном экране (вместо рекламы) создай блок "Инвестиции".
   - Пользователь может вложить туда свои "шпроты". Доходность — 1% в день от суммы вклада.
   - Сделай визуальный счетчик: начисление процентов должно отображаться в реальном времени! Баланс должен плавно увеличиваться каждую миллисекунду прямо на глазах у пользователя (используй requestAnimationFrame или setInterval на фронтенде для плавной анимации цифр). При этом бэкенд должен точно рассчитывать баланс по времени, прошедшему с момента вклада, чтобы избежать накруток.

Пожалуйста, реализуй эти изменения, обнови стейты, закоммить и сделай push в GitHub!