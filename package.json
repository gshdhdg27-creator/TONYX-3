{
  "name": "adsgram-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "api-server",
    "mini-app",
    "db",
    "api-client-react",
    "api-zod",
    "api-spec"
  ],
  "scripts": {
    "dev:api": "cd api-server && PORT=3001 npm run dev",
    "dev:app": "cd mini-app && npm run dev",
    "db:push": "cd db && npm run push",
    "dev": "npx concurrently \"npm run dev:api\" \"npm run dev:app\"",
    "build": "npm run build --workspaces --if-present",
    "start": "npx concurrently \"PORT=3001 node api-server/dist/index.js\" \"cd mini-app && npx serve -l 3000 dist/public\""
  }
}
